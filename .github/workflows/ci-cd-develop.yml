name: Gateway server develop CI/CD Pipeline

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Code checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission to gradlew
        run: chmod +x gradlew

      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Check environment variables
        run: echo "SPRING_PROFILES_ACTIVE=$SPRING_PROFILES_ACTIVE"

      - name: Build with Gradle
        run: ./gradlew build -DSPRING_PROFILES_ACTIVE=test

      - name: Run tests
        run: ./gradlew test -DSPRING_PROFILES_ACTIVE=test
